# 🌌 My Arch Linux Hyprland Dotfiles

Моя персональная конфигурация для Arch Linux.
Основана на оконном менеджере **Hyprland** с использованием **LazyVim** для разработки.

![Preview](preview.png)

## 🛠 Что внутри? (Разбор стека)

Здесь описано, какие компоненты используются и зачем они нужны.

### 🖥 Графическое окружение
| Компонент | Название | Зачем нужен? |
| :--- | :--- | :--- |
| **Window Manager** | `Hyprland` | Сам оконный менеджер. Отвечает за окна, анимации и управление. |
| **Status Bar** | `Waybar` | Верхняя панель (часы, workspaces, трей, загрузка CPU). |
| **Notifications** | `Dunst` | Красивые всплывающие уведомления. |
| **Launcher** | `Rofi` (Wayland) | Меню запуска приложений (аналог "Пуск" или Spotlight). |
| **Wallpaper** | `swww` | Демон для установки обоев (эффективный и быстрый). |
| **Lock Screen** | `hyprlock` | Экран блокировки (если настроен). |

### ⌨️ Терминал и Редактор
| Компонент | Название | Зачем нужен? |
| :--- | :--- | :--- |
| **Terminal** | `Foot` | Очень быстрый и легкий терминал для Wayland. |
| **Shell** | `Fish` | Удобная командная строка с автодополнениями "из коробки". |
| **Prompt** | `Starship` | Красивая строка приглашения в терминале (показывает git ветки, версию python и т.д.). |
| **Editor** | `Neovim` (LazyVim) | Мощный текстовый редактор. Используется сборка LazyVim. |

### 📂 Утилиты (Зависимости)
Эти программы нужны для работы скриптов, превью файлов и плагинов Neovim.

| Пакет | Зачем нужен? |
| :--- | :--- |
| `stow` | Утилита для управления dotfiles (создает симлинки). |
| `yazi` | Консольный файловый менеджер (как Ranger, но быстрее). |
| `ripgrep` | Супер-быстрый поиск текста. **Нужен для работы поиска в LazyVim**. |
| `fd` | Быстрый поиск файлов. **Нужен для LazyVim**. |
| `jq` | Утилита для работы с JSON. Нужна для скриптов погоды или Hyprland. |
| `grim` + `slurp` | Инструменты для создания скриншотов (весь экран или область). |
| `wl-clipboard` | Позволяет копировать/вставлять текст из консоли в буфер обмена. |
| `ffmpegthumbnailer` | Позволяет Yazi показывать превью видео-файлов. |
| `poppler` | Позволяет Yazi показывать превью PDF. |
| `imagemagick` | Обработка изображений для превью. |
| `ttf-jetbrains-mono-nerd` | Шрифт. Без него иконки везде (в баре, в nvim) будут сломаны. |

---

## 🚀 Установка

### Вариант 1: Автоматическая (Скрипт)
Скрипт сам установит пакеты через `pacman`, сделает резервные копии ваших текущих конфигов и настроит ссылки.

1. Клонируйте репозиторий:
   ```bash
   git clone [https://github.com/shalyn42k/dotfiles.git](https://github.com/shalyn42k/dotfiles.git) ~/dotfiles
   cd ~/dotfiles
