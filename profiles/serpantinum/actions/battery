#!/usr/bin/env bash
# Отдельного виджета батареи нет: заряд показан внутри системной панели.
# Этот файл заодно чинит SUPER+B: контракт грузится последним и снимал
# риговый бинд панели, а подсказка отсылала к самой себе.
exec "$(dirname "$(readlink -f "$0")")/../shell/bin/serpantinum" msg toggle system
