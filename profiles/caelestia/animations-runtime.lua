-- Анимации рига caelestia для применения в живую lua-сессию (hyprctl eval).
-- Сам конфиг standalone — просто перевыполняем его.
dofile(os.getenv("HOME") .. "/rigger/profiles/caelestia/hypr/hyprland/animations.lua")
