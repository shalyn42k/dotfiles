-- hypr-user.lua — точка расширения caelestia-шелла для пользовательских
-- hypr-настроек. Бинды отсюда уехали.
--
-- Раньше файл держал копию кросс-риг контракта (KEYBINDS.md §2.1), а вторая
-- копия жила в наборе другого рига — их приходилось править в двух местах.
-- Теперь контракт один:
--
--     .config/hypr-shared/contract-binds.lua
--
-- Он применяется последним при загрузке конфига любого рига и принадлежит
-- владельцу "shared", поэтому переживает переключение ригов. Подробности —
-- docs/superpowers/specs/2026-09-02-rig-switch-binds-ownership-design.md
--
-- Файл оставлен пустым намеренно: caelestia сорсит его сам, и его отсутствие
-- пришлось бы обрабатывать отдельно.
